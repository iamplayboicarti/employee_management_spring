spring:
    application:
        name: employee_spring_management_test
    data:
        web:
            pageable:
                one-indexed-parameters: true
    datasource:
        url: jdbc:mysql://localhost:3306/hr_management_test?allowPublicKeyRetrieval=true&useSSL=false
        username: root
        password: 123456
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
    servlet:
        multipart:
            max-file-size: 5MB
            max-request-size: 10MB
jwt:
    secret: tuan_dao_jwt_secret_key_HS256_tuan_dao_jwt_secret_key_HS256_tuan_dao_jwt_secret_key_HS256
    expiration: 86400000
    refresh-expiration: 604800000

app:
    upload:
        base-dir: ./uploads-test
        max-file-size: 5242880
        allowed-extensions:
            - jpg
            - jpeg
            - png
            - gif
            - webp
        allowed-folders:
            - avatars
            - logos
